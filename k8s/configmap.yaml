# Nginx configmap
apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-configmap
data:
  nextjs.conf: |
    # NGINX configuration for Nextjs App
    server {
      listen 80;

      location / {
          proxy_pass http://nextjs-service;
      }                                                                                                       
    }